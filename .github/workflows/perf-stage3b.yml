name: TrustLine performance stage 3b

on:
  push:
    branches: [main]

jobs:
  optimize:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - name: Install from lockfile
        run: npm ci
      - name: Lazy load AuthScreen
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path
          p = Path('src/App.tsx')
          s = p.read_text(encoding='utf-8-sig')
          old = 'import { AuthScreen } from "./components/AuthScreen";'
          new = 'const AuthScreen = lazy(() => import("./components/AuthScreen").then((m) => ({ default: m.AuthScreen })));'
          if old not in s:
              raise SystemExit('AuthScreen import pattern not found')
          s = s.replace(old, new, 1)
          p.write_text(s, encoding='utf-8')
          PY
      - name: Build verification
        run: npm run build
      - name: Commit optimization and remove workflow
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm -f .github/workflows/perf-stage3b.yml
          git add src/App.tsx
          git commit -m "perf: lazy load AuthScreen"
          git push origin main
